[package]
name = "webkit-web-transport"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1.0"
cxx = "1.0"
quinn-proto = { version = "0.11", features = ["platform-verifier"] }
rand = "0.9"
ring = "0.17.14"

[build-dependencies]
cxx-build = "1.0"

[lib]
crate-type = ["staticlib"]
